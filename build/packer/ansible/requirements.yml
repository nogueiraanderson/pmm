---
# Collections the agent-aws.yml playbook needs on the CONTROL node (the
# GitHub runner). The playbook's own ansible-galaxy task installs collections
# on the baked image for jobs that run Ansible there, not here. Pinned exact.
collections:
  - name: community.docker
    version: "5.3.0"
  - name: community.general
    version: "13.4.0"
  - name: ansible.posix
    version: "2.2.2"
